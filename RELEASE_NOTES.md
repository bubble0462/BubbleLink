# BubbleLink Studio · 测试版发布说明

**版本**：v0.9.1-beta（2026-08-27）
**性质**：功能完整版的首轮硬件实测测试版（v0.9.0 的缺陷修复版）

---

## 本版修复（相对 v0.9.0-beta）

### 烧录器识别（重要）
- **改用 Windows PnP 枚举（cfgmgr32）作为主识别来源**：经典 ST-Link/V2
  （0483:3748，只有厂商类调试接口、无 CDC/HID）此前会显示"未识别到烧录器"，
  现在可可靠识别。CDC/HID 降级为序列号补充与兜底。
- ST-Link PID 家族按 TN1235 补全：新增 3752（V2-1）、3753/3754（V3 带/无
  bridge）、3757（V3PWR）
- 同一设备不再被 CDC + HID 重复计数（此前一台 DAPLink 可能显示 ×2）

### 目标芯片自动识别（重要）
- **修正 STM32F1 DEV_ID 映射颠倒**：0x418 = 互联型（F105/F107）、
  0x430 = 超大容量 XL（F103xF/xG）——此前两者写反，识别结果完全颠倒
- 识别不再执行 `go`：连接不暂停内核、断开不恢复（pyOCD attach 模式），
  **识别过程完全只读**，不影响正在运行的目标程序
- 识别失败时透传 pyOCD stderr 错误详情（此前被丢弃）

### 串口终端
- 修复"数据已到达但终端画面停留在旧内容"的刷新缺陷
- 页面切换保活：串口连接、录制路径、烧录进度与日志在来回切页后完整保留
- 录制落盘：覆盖式写入（此前选同名文件会追加旧数据）、250ms 周期刷盘、
  写盘失败自动停止并弹提示、仅成功写入才计数
- 发送队列有界化（1024 条）：设备收数不及时提示"队列已满"，不再无限堆积内存
- 端口友好名扫描范围收敛到 `Enum\USB` 子树，降低后台注册表开销

### 固件解析与安全
- Intel HEX 支持 type 02（扩展段地址）、零长记录防下溢
- CRC32 明确标注为**载荷 CRC32**（按文件记录顺序计算，不等于 Flash 回读 CRC）
- 启用 CSP 内容安全策略；隐藏页面暂停设备轮询

## 安装

- **安装包**（推荐）：`BubbleLink_Studio_0.9.1_beta_setup.exe` — 双击安装，中文界面，
  当前用户安装（**不需要管理员权限**），自动创建桌面 + 开始菜单快捷方式
- **便携版**：`BubbleLink_Studio_0.9.1_beta_portable.exe` — 单文件直接双击运行，不写注册表

两者均依赖系统 WebView2（Win10/11 自带）。

## 卸载

- 安装版：Windows「设置 → 应用」里卸载，或开始菜单里的 Uninstall
- 便携版：直接删除文件

## 功能总览

### 串口终端（对应烧录器模式 01 DAPLink 的 CDC 串口）
端口自动枚举（友好名称）、波特率 1200–921600、UTF-8/GBK、按字节间隙分包（默认 20ms）、
时间戳与分包字节数标注、HEX/文本显示切换、正则过滤、高亮规则（内置 ERROR/WARN/OK/地址
+ 自定义正则持久化）、发送（文本/HEX、回车换行、SUM/CRC8/CRC16-MODBUS 校验、定时循环、
↑↓ 历史）、接收数据原始落盘、日志导出、会话统计、DTR/RTS 实时控制。

### 烧录工作台（模式 00 / 01）
三模设备自动识别（每 2 秒探测 + 手动刷新）、固件解析（地址范围/载荷 CRC32）、目标芯片自动
识别（读 DBGMCU_IDCODE + Flash 容量，推断密度与型号）、双引擎自动分发：

| 烧录器模式 | 引擎 | 芯片型号 |
|---|---|---|
| 01 DAPLink | pyOCD（需 `pip install pyocd`） | 自动识别 / 手动选择 |
| 00 ST-Link | STM32CubeProgrammer CLI（自动探测安装路径） | 引擎自动识别 |
| 10 BMP | 不烧录，提示走 gdb/VS Code 工作流 | — |

擦除方式（全片/扇区）、烧后校验、完成自动复位运行、进度条（擦除/写入/校验阶段）、
引擎日志实时着色、任务取消。

### 界面
深蓝白简洁主题；组件（光斑卡片/环形进度/自定义下拉/刷新按钮/Toast/数字滚动/骨架屏）
适配自个人组件库实现。

## 已知限制（测试版）

1. BMP 模式不支持页面内烧录（设计如此，走 gdb）
2. 目标芯片自动识别对 F1 系列给出"密度+容量"级推断，精确型号标注"推测"
3. ST-Link 模式未接序列号选择（多个 ST-Link 同时在线时用第一个）
4. 烧录器自身固件升级（模式 11 恢复口自刷机）未实现（二期）
5. 长时间串口收流的前端只渲染最近 1500 行（完整缓冲可导出）

## 实测反馈清单（请按此验证）

- [ ] 01 模式：识别徽章亮起 → 选 .hex → 烧录成功且目标复位运行
- [ ] 01 模式：目标芯片自动识别徽章（推测型号）正确（高密度 F103 板重点验证）
- [ ] 00 模式：切到 ST-Link 后徽章能亮起（经典 V2 无串口也能识别）→ 烧录成功
- [ ] 00 模式识别后目标板程序未被暂停/复位
- [ ] 串口终端：连接 CDC 口收日志、高亮/过滤/导出/落盘；切到烧录页再切回，连接仍在
- [ ] 下拉菜单各处不被遮挡、滚轮在菜单内可滚动列表；刷新按钮旋转动画正常
